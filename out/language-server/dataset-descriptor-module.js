"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createDatasetDescriptorServices = exports.DatasetDescriptorModule = void 0;
const langium_1 = require("langium");
const module_1 = require("./generated/module");
const dataset_descriptor_validator_1 = require("./dataset-descriptor-validator");
const dataset_descriptor_scope_1 = require("./dataset-descriptor-scope");
const dataset_descriptor_naming_1 = require("./dataset-descriptor-naming");
/**
 * Dependency injection module that overrides Langium default services and contributes the
 * declared custom services. The Langium defaults can be partially specified to override only
 * selected services, while the custom services must be fully specified.
 */
exports.DatasetDescriptorModule = {
    validation: {
        ValidationRegistry: (services) => new dataset_descriptor_validator_1.DatasetDescriptorValidationRegistry(services),
        DatasetDescriptorValidator: () => new dataset_descriptor_validator_1.DatasetDescriptorValidator()
    },
    references: {
        ScopeComputation: (services) => new dataset_descriptor_scope_1.DatasetDescriptorScopeComputation(services),
        NameProvider: () => new dataset_descriptor_naming_1.DatasetDescriptorNameProvider()
    },
};
/**
 * Create the full set of services required by Langium.
 *
 * First inject the shared services by merging two modules:
 *  - Langium default shared services
 *  - Services generated by langium-cli
 *
 * Then inject the language-specific services by merging three modules:
 *  - Langium default language-specific services
 *  - Services generated by langium-cli
 *  - Services specified in this file
 *
 * @param context Optional module context with the LSP connection
 * @returns An object wrapping the shared services and the language-specific services
 */
function createDatasetDescriptorServices(context) {
    const shared = (0, langium_1.inject)((0, langium_1.createDefaultSharedModule)(context), module_1.DatasetDescriptorGeneratedSharedModule);
    const DatasetDescriptor = (0, langium_1.inject)((0, langium_1.createDefaultModule)({ shared }), module_1.DatasetDescriptorGeneratedModule, exports.DatasetDescriptorModule);
    shared.ServiceRegistry.register(DatasetDescriptor);
    return { shared, DatasetDescriptor };
}
exports.createDatasetDescriptorServices = createDatasetDescriptorServices;
//# sourceMappingURL=dataset-descriptor-module.js.map